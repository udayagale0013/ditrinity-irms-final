'use client';
import { useEffect,useState } from 'react';
import { Users, UserCheck, ClipboardList, FileText, Clock3, Globe2, TrendingUp, Download, ArrowUpRight, CalendarDays } from 'lucide-react';

type D={metrics:any;charts:any;recent:any[]};
export default function Dashboard(){const [d,setD]=useState<D|null>(null),[err,setErr]=useState('');useEffect(()=>{fetch('/api/dashboard').then(r=>r.json()).then(x=>x.ok?setD(x):setErr(x.error?.message||'Dashboard failed'))},[]);if(err)return <div className="notice danger-notice">{err}</div>;if(!d)return <div className="card empty">Loading live dashboard from PostgreSQL…</div>;const m=d.metrics;const cards=[['Total Resources',m.resources,Users],['Open Requirements',m.openRequirements,ClipboardList],['Active Allocations',m.activeAllocations,UserCheck],['Open SOWs',m.openSows,FileText],['Pending Timesheets',m.pendingTimesheets,Clock3]];return <><div className="page-head"><div><h1>Dashboard</h1><p>Live operational view · data is read from Railway PostgreSQL. No seeded dashboard numbers.</p></div><a className="btn primary" href="/api/dashboard?format=csv"><Download size={13}/> Download Report</a></div><div className="kpi-row">{cards.map(([label,val,Icon]:any)=><div className="card metric" key={label}><div><div className="label">{label}</div><div className="value">{val}</div><div className="delta">Live database value</div></div><div className="metric-icon"><Icon size={16}/></div></div>)}</div><div className="grid grid-3" style={{marginTop:14}}><div className="card"><h3 className="section-title">Utilization & Capacity</h3><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:15,alignItems:'center'}}><div className="donut-wrap"><div className="donut"></div><div className="donut-center">{m.utilization.toFixed(1)}<small style={{display:'block',fontSize:8}}>% allocated</small></div></div><div style={{fontSize:11,lineHeight:2}}><div>Active resources <b>{m.resources}</b></div><div>External resources <b>{m.externalResources}</b></div><div>Pipeline leads <b>{m.pipelineLeads}</b></div><div>Available capacity <b>{m.capacityAvailable.toFixed(1)}%</b></div></div></div></div><div className="card"><h3 className="section-title">Requirement Trend</h3><div className="chart">{d.charts.requirementsByMonth.length?d.charts.requirementsByMonth.map((x:any)=><div key={x.label} className="bar" style={{height:`${Math.max(8,Math.min(100,x.value*10))}%`}}><span>{x.label}<br/>{x.value}</span></div>):<div className="empty">No future requirements recorded.</div>}</div></div><div className="card"><h3 className="section-title">Quick Access</h3><div className="quick-actions"><a className="quick" href="/resources"><Users size={17}/>Resources</a><a className="quick" href="/requirements"><ClipboardList size={17}/>Requirements</a><a className="quick" href="/timesheets"><Clock3 size={17}/>Timesheets</a><a className="quick" href="/sow"><FileText size={17}/>SOW</a><a className="quick" href="/pipeline"><TrendingUp size={17}/>Forecast</a><a className="quick" href="/powerbi"><ArrowUpRight size={17}/>Power BI</a><a className="quick" href="/integrations"><Globe2 size={17}/>Integrations</a><a className="quick" href="/admin"><CalendarDays size={17}/>Data</a></div></div></div><div className="grid grid-2" style={{marginTop:14}}><div className="card"><h3 className="section-title">Resource Distribution</h3><div className="table-wrap"><table className="data-table"><thead><tr><th>Entity</th><th>Resources</th></tr></thead><tbody>{d.charts.resourceByEntity.map((x:any)=><tr key={x.label}><td>{x.label}</td><td>{x.value}</td></tr>)}</tbody></table></div></div><div className="card"><h3 className="section-title">Recent Activities</h3>{d.recent.length?d.recent.map((x:any,i)=><div key={i} className="integration-status"><div><span className="status-dot on"></span>{x.action} · {x.entity_type}</div><small style={{color:'#8a95a4'}}>{new Date(x.created_at).toLocaleString()}</small></div>):<div className="empty">Audit events will appear after real changes.</div>}</div></div></>}
