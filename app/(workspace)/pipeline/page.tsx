import EntityPanel from '@/components/EntityPanel';
export default function Pipeline(){return <><div className="page-head"><div><h1>Pipeline & Forecast</h1><p>Controlled pipeline demand with certainty bands 100 / 75 / 50 / 25 and skill/capacity gap visibility.</p></div></div><div className="notice" style={{marginBottom:12}}>For the current build, pipeline records can be entered directly. SharePoint Excel ingestion is provided as an integration boundary; configure Microsoft Graph credentials before enabling live ingestion.</div><div className="grid" style={{gap:12}}><EntityPanel entity="pipeline_leads"/><EntityPanel entity="forecasts"/></div></>}
