import EntityPanel from '@/components/EntityPanel';
import { ENTITIES, EXTENDED_ENTITIES } from '@/lib/entities';
export default function Admin(){return <><div className="page-head"><div><h1>Admin & Data Management</h1><p>Live CRUD over the deployed Railway PostgreSQL schema. The first section is the 41-table baseline; extensions are additive production controls.</p></div></div><div className="notice" style={{marginBottom:14}}>No seed/demo records are included. Create real users, resources, accounts, requirements, SOWs, approvals, timesheets and configuration here. Protected state changes should use the dedicated workflow actions, not raw CRUD.</div><div className="grid" style={{gap:12}}>{ENTITIES.map(e=><EntityPanel key={e} entity={e}/>)}</div><div style={{height:25}}/><h2 style={{fontSize:17}}>Production extensions</h2><p style={{fontSize:11,color:'#778392'}}>Run <code>db_extensions.sql</code> once against Railway before using these capabilities.</p><div className="grid" style={{gap:12}}>{EXTENDED_ENTITIES.map(e=><EntityPanel key={e} entity={e}/>)}</div></>}
