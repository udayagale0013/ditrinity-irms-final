'use client';
import {useState} from 'react';
export default function Matching(){const [id,setId]=useState(''),[rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState(''),[loading,setLoading]=useState(false);async function run(){setLoading(true);setMsg('');const r=await fetch('/api/matching/run',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({requirementId:id})});const d=await r.json();if(r.ok)setRows(d.results);else setMsg(d.error?.message||'Matching failed');setLoading(false)}return <><div className="page-head"><div><h1>AI Resource Matching</h1><p>Deterministic eligibility first, AI semantic recommendation second, RM remains the decision maker.</p></div></div><div className="card"><div className="toolbar"><div><b style={{fontSize:12}}>Run a real requirement</b><div style={{fontSize:10,color:'#8792a0'}}>The engine reads requirements, required skills, resource skills and current allocations from PostgreSQL.</div></div><div className="toolbar-right"><input value={id} onChange={e=>setId(e.target.value)} placeholder="Requirement UUID"/><button className="btn primary" disabled={!id||loading} onClick={run}>{loading?'Running…':'Run matching'}</button></div></div>{msg&&<div className="notice danger-notice">{msg}</div>}{rows.length>0&&<div className="table-wrap" style={{marginTop:12}}><table className="data-table"><thead><tr><th>Rank</th><th>Resource</th><th>Experience</th><th>Allocated %</th><th>Rule</th><th>AI</th><th>Final</th><th>Reason</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.rank}</td><td><b>{x.name}</b><br/><small>{x.resource_code}</small></td><td>{x.experience_years??'-'}</td><td>{x.allocated_percent}</td><td>{x.rule_score}</td><td>{x.ai_score??'—'}</td><td><b>{x.final_score}</b></td><td style={{whiteSpace:'normal',minWidth:250}}>{x.recommendation_reason}</td></tr>)}</tbody></table></div>}</div></>}
