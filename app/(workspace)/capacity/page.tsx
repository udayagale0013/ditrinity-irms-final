import EntityPanel from '@/components/EntityPanel';
export default function Capacity(){return <><div className="page-head"><div><h1>Capacity, Utilization & Bench</h1><p>Capacity snapshots and active allocation percentages. Concurrent allocations above 100% remain visible and are flagged.</p></div></div><div className="grid grid-2"><EntityPanel entity="capacity_snapshots"/><div className="card"><h3 className="section-title">Calculation rule</h3><div className="notice">Utilization is derived from active allocations. Bench is the available portion of active resources. Do not manually overwrite the derived dashboard; use allocations/availability as the source.</div></div></div></>}
